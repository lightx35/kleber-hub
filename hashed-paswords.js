const bcrypt = require('bcrypt');


//Lylian

(async () => {
  const password = "Lylian"; // ton mot de passe en clair
  const hashed = await bcrypt.hash(password, 10); // 10 = "salt rounds"
  console.log("Mot de passe :", password);
  console.log("Hash bcrypt :", hashed);
})();

//Sam

(async () => {
  const password = "Sam"; // ton mot de passe en clair
  const hashed = await bcrypt.hash(password, 10); // 10 = "salt rounds"
  console.log("Mot de passe :", password);
  console.log("Hash bcrypt :", hashed);
})();

//Nicolas

(async () => {
  const password = "Nicolas"; // ton mot de passe en clair
  const hashed = await bcrypt.hash(password, 10); // 10 = "salt rounds"
  console.log("Mot de passe :", password);
  console.log("Hash bcrypt :", hashed);
})();

//Noha

(async () => {
  const password = "Noha"; // ton mot de passe en clair
  const hashed = await bcrypt.hash(password, 10); // 10 = "salt rounds"
  console.log("Mot de passe :", password);
  console.log("Hash bcrypt :", hashed);
})();

//Naophël

(async () => {
  const password = "Naophël"; // ton mot de passe en clair
  const hashed = await bcrypt.hash(password, 10); // 10 = "salt rounds"
  console.log("Mot de passe :", password);
  console.log("Hash bcrypt :", hashed);
})();

//Nathan

(async () => {
  const password = "Nathan"; // ton mot de passe en clair
  const hashed = await bcrypt.hash(password, 10); // 10 = "salt rounds"
  console.log("Mot de passe :", password);
  console.log("Hash bcrypt :", hashed);
})();

//Louis

(async () => {
  const password = "Louis"; // ton mot de passe en clair
  const hashed = await bcrypt.hash(password, 10); // 10 = "salt rounds"
  console.log("Mot de passe :", password);
  console.log("Hash bcrypt :", hashed);
})();

//Andreas

(async () => {
  const password = "Andreas"; // ton mot de passe en clair
  const hashed = await bcrypt.hash(password, 10); // 10 = "salt rounds"
  console.log("Mot de passe :", password);
  console.log("Hash bcrypt :", hashed);
})();